# Optional local development port for the Vite frontend.
# The default is 5173 when PORT is not set.
PORT=5173

# Optional local backend port when commands are run inside server/.
# The default used by server/src/index.ts is 3001.
# PORT=3001
