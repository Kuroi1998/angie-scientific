import React from 'react';

// Stub component for Phase 5 (Three.js / R3F)
export const AtomicModel3D: React.FC<{ atomicNumber: number }> = ({ atomicNumber }) => {
  return (
    <div className="atomic-model-3d-placeholder">
      {/* Phase 5 implementation */}
      <p>Modèle 3D en préparation pour l'élément {atomicNumber}</p>
    </div>
  );
};
